name: Insecure Workflow Example

on:
  push:

permissions: write-all

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unsafe install
        run: curl https://example.invalid/install.sh | sh
      - name: Hardcoded secret example
        run: echo "api_key=ABCDEF1234567890"
